# Case Types
camelCase for for variables, functions, and class methods. 
PascalCase for classes, types, and interfaces. 
snake_case turns for database schemas.
UPPER_CASE for constants that act like configuration.

# Multi Command separator
you cannot use && when runnning multiple commands. you MUST use ; 
you do not need to switch directories every time you run a command. 