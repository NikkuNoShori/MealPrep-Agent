# MOP-0008: Chat — Intent Router → Tool-Using Single Agent

| Field | Value |
|-------|-------|
| **MOP** | MOP-0008 |
| **Title** | Chat: Intent Router → Tool-Using Single Agent |
| **Date Submitted** | 2026-06-01 |
| **Date Updated** | 2026-06-15 (Tier 1 golden-routing tests verified; promoted to complete) |
| **Date Completed** | 2026-06-15 |
| **Submitted By** | Nick Neal |
| **Status** | complete |

> **Shipped as of 2026-06-02:**
> - **Step 1:** `chatWithTools` method on `_shared/openrouter-client.ts` (OpenAI-compatible tools + tool_choice)
> - **Step 2:** `RAG_RESPONSE_PROMPT` migrated to `_shared/recipe-prompts.ts`
> - **Step 3:** `CHAT_AGENT_SYSTEM_PROMPT` + `SUBSTITUTION_PROMPT` added to registry; `INTENT_DETECTION_PROMPT` marked `@deprecated`
> - **Step 4:** Tool catalog + dispatcher in `chat-api/tools/{catalog,dispatch}.ts` — **12 tools** (11 from original design + `web_search_recipe` per Addendum 1). Recursive `user_id` rejection. Capability gate via `available()` (web_search hidden when `WEB_SEARCH_API_KEY` unset).
> - **Step 5:** 12 handlers in `chat-api/tools/handlers.ts`. All DB-backed handlers use `ctx.supabase` (user-scoped RLS); `web_search_recipe` delegates to new `_shared/web-search-client.ts` (Tavily default, Brave/Serper drop-in).
> - **Step 6:** `chat-api/agent-loop.ts` with `MAX_ITERS = 5`, `<tool_result>` wrapping for prompt-injection defense, destructive-tool short-circuit.
> - **Step 7:** `chat-api/index.ts` wired to agent loop with `context.confirmAction` short-circuit path.
> - **Step 8 (complete as of 2026-06-03):** Backend contract + Frontend UI shipped.
>   - Backend: `pendingConfirmation` types + `confirmAction` request field in `src/services/api.ts`; contract tests in `src/services/__tests__/chat-agent.test.ts`.
>   - Frontend: new `src/components/chat/ConfirmationPrompt.tsx` (accessible `role="alertdialog"`, amber-on-light/dark warning styling, busy state with spinner). Wired into `src/components/chat/ChatInterface.tsx` in both message-render paths (recipe display + standard bubble). `Message` interface widened with optional `pendingConfirmation`; new `confirmingMessageId` state; `handleConfirmAction` dispatches `apiClient.sendMessage` with `context.confirmAction`, appends the result as a new AI message; `handleCancelConfirmation` dismisses locally with no backend call.
>   - Tests: 5 new component tests in `src/components/chat/__tests__/ConfirmationPrompt.test.tsx` (summary + tool name render, confirm/cancel callbacks fire, in-flight state disables both buttons + swaps confirm label to "Confirming…", accessible alertdialog name).
> - **Step 9:** Tests — `chat-api/__tests__/agent-loop.test.ts` (Deno, scripted tool-call assertions including web_search dispatch + catalog gating + NO_RESULTS mapping), `src/services/__tests__/chat-agent.test.ts` (Vitest+MSW, 4 contract tests), `__tests__/fixtures/golden.json` (30-prompt evaluation set).
> - **Step 10:** Docs — `docs/ARCHITECTURE.md` (agent-loop diagram + tool-catalog table + WEB_SEARCH env vars), `docs/API.md` (`pendingConfirmation` + `confirmAction` documented, prompt registry refreshed).
>
> **Outstanding:**
> - **Output-schema validation on handlers (P2 deviation, optional):** dispatcher shape-checks `{ok, error, requiresConfirmation}` envelopes but does not run JSON-Schema validation on each handler's `data` payload. Acceptable today (TypeScript types enforce shape at compile time); revisit if downstream consumers depend on stricter contracts.
> - **Verification block `human` items:** latency/cost/golden-set/prompt-injection/graceful-degradation evals require staging or production traffic. Not machine-checkable today. **Roadmap:** [MOP-0008-ADVISEMENT.md](MOP-0008-ADVISEMENT.md) (Tier 1 golden-routing tests + short-form video intake phases).
>
> **Next status transition:** `complete` as of 2026-06-15 (`/verify-mop MOP-0008` passed). Live eval remains in Manual Follow-up.

---

## Summary

Replace `chat-api`'s single-shot intent detection (one LLM call classifies → routes to one of three handlers) with a tool-using agent: one LLM call receives the user message + a tool catalog and decides which tools to invoke (zero, one, many, or chained). This unblocks multi-step queries like "find my chicken recipes safe for the kids and add two to next week's plan" that the current router cannot handle.

This is **Phase 1** of the AI agent migration from `docs/AI_INTEGRATION_AUDIT.md`. Phase 2 (supervisor + specialists) is intentionally deferred until evals show single-agent insufficient.

---

## Architectural Background

- **Current pattern:** Routing (Anthropic's pattern taxonomy). Limit: one intent per turn.
- **Target pattern:** Agent / autonomous tool use (Anthropic). Single LLM in a loop with a tool catalog.
- **Pattern choice rationale:** See `.claude/agents/cooking-bot-knowledge/architecture-patterns.md` §6. Supervisor + specialists is overkill until measurement justifies it.
- **Designed by:** `cooking-bot-architect` subagent (2026-06-01). Full design at [MOP-0008-design.md](MOP-0008-design.md).
- **Tool catalog:** 12 tools (read: 7, capture: 1, plan/cart: 2, destructive: 2). Under the "30+ tool" anti-pattern threshold. **Addendum 1** (this document, below) adds `web_search_recipe` to the design's original 11; the design doc remains the source for tools 1–11.
- **Model:** `qwen/qwen-2.5-7b-instruct`, t=0.2, tool_choice=auto. Max 5 iterations per turn.

---

## Scope of Work

> Concrete steps from [MOP-0008-design.md](MOP-0008-design.md), ordered by dependency.

### Step 1: `chatWithTools` on the OpenRouter client
**Files affected:** `supabase/functions/_shared/openrouter-client.ts`

Add `chatWithTools(systemPrompt, messages, tools, model, options) → { content, tool_calls[] }`. POST to OpenRouter `/chat/completions` with OpenAI-compatible `tools` + `tool_choice` (Qwen 2.5 supports the format via OpenRouter passthrough). Export `ChatMessage` and `ToolSpec` types.

### Step 2: Move `RAG_RESPONSE_PROMPT` into the registry
**Files affected:** `supabase/functions/_shared/recipe-prompts.ts`, `supabase/functions/chat-api/index.ts`

Extract the inline prompt from `chat-api/index.ts:184–193` into the shared registry.

### Step 3: Add agent prompts to the registry
**Files affected:** `supabase/functions/_shared/recipe-prompts.ts`

Append `CHAT_AGENT_SYSTEM_PROMPT` (Chef Marcus + 6 hard rules) and `SUBSTITUTION_PROMPT` (used by the substitution tool). Mark `INTENT_DETECTION_PROMPT` `@deprecated`.

### Step 4: Tool catalog + dispatcher
**Files affected:** `supabase/functions/chat-api/tools/catalog.ts` (new), `supabase/functions/chat-api/tools/dispatch.ts` (new)

`catalog.ts` exports `TOOL_CATALOG`, `DESTRUCTIVE_TOOLS`, `CONDITIONALLY_DESTRUCTIVE`. `dispatch.ts` exports `dispatchTool(name, args, ctx)` that schema-validates args via Ajv (or a 60-line subset checker), looks up the handler, validates output, and returns a typed `ToolResult`.

### Step 5: Tool handlers (12 tools)
**Files affected:** `supabase/functions/chat-api/tools/handlers.ts` (new), `supabase/functions/_shared/web-search-client.ts` (new — Addendum 1)

One exported function per tool. All DB-backed handlers use the user-scoped `ctx.supabase` so RLS enforces visibility. No handler accepts `user_id` from args — it comes from JWT. Extraction handler invokes existing `recipe-pipeline/extract-only` (no re-implementation). The `web_search_recipe` handler (Addendum 1) delegates to the new shared `web-search-client.ts` and has no DB surface.

### Step 6: Agent loop
**Files affected:** `supabase/functions/chat-api/agent-loop.ts` (new)

`runAgentLoop(input, ctx) → AgentReply` with `MAX_ITERS = 5`. Wraps tool outputs in `<tool_result>` markers (defense-in-depth against tool-output prompt injection). Destructive tools short-circuit with `pendingConfirmation` envelope.

### Step 7: Wire into `handleSendMessage`
**Files affected:** `supabase/functions/chat-api/index.ts`

Replace lines 485–532 (router) with `runAgentLoop` call. Add `confirmAction` short-circuit path. Remove `detectIntent`, `handleRAGSearch`, `extractRecipe`, `handleGeneralChat`. Keep `handleGetHistory`, `handleClearHistory`, image upload.

### Step 8: Frontend contract
**Files affected:** `src/services/api.ts`, `src/components/chat/*`

Add `pendingConfirmation` to response types and `confirmAction` to request `context`. Chat UI renders confirmation as an inline message with Confirm/Cancel.

### Step 9: Tests
**Files affected:** `supabase/functions/chat-api/__tests__/agent-loop.test.ts` (new, Deno test), `src/services/__tests__/chatAgent.test.ts` (new, Vitest+MSW), `supabase/functions/chat-api/__tests__/fixtures/golden.json` (new)

30-prompt golden set (10 single-intent, 10 multi-intent, 10 destructive-confirm). See `Verification` block.

### Step 10: Docs
**Files affected:** `docs/ARCHITECTURE.md`, `docs/API.md`

Update chat-api flow diagram; document `pendingConfirmation` and `confirmAction` on the API.

---

## Priority

| Priority | Item | Effort | Impact |
|----------|------|--------|--------|
| P0 | Tool catalog definition + schemas | M | High |
| P0 | Agent loop in `chat-api` | M | High |
| P0 | Confirmation flow for destructive tools | S | High (safety) |
| P0 | Eval harness (golden test set) | M | High (prevents regressions) |
| P1 | Frontend confirmation UI | S | Med |
| P1 | Inline prompt migration to registry | S | Med |

---

## Verification

> **Lockticket-format acceptance criteria.** Each criterion is machine-checkable except where marked `[human]`. The verification skill (MOP-0010) will run these mechanically once available; until then, the qa-auditor reviews them by hand.

```yaml
verification:
  # --- Static structural checks ---
  - id: chat-with-tools-method
    type: grep
    path: supabase/functions/_shared/openrouter-client.ts
    pattern: 'chatWithTools'
    expect: present

  - id: agent-system-prompt-in-registry
    type: grep
    path: supabase/functions/_shared/recipe-prompts.ts
    pattern: 'CHAT_AGENT_SYSTEM_PROMPT'
    expect: present

  - id: rag-prompt-in-registry
    type: grep
    path: supabase/functions/_shared/recipe-prompts.ts
    pattern: 'RAG_RESPONSE_PROMPT'
    expect: present

  - id: no-inline-rag-prompt
    type: grep
    path: supabase/functions/chat-api/index.ts
    pattern: 'RAG_RESPONSE_PROMPT\s*='
    expect: absent

  - id: tool-catalog-exists
    type: file-exists
    path: supabase/functions/chat-api/tools/catalog.ts

  - id: tool-dispatcher-exists
    type: file-exists
    path: supabase/functions/chat-api/tools/dispatch.ts

  - id: tool-handlers-exist
    type: file-exists
    path: supabase/functions/chat-api/tools/handlers.ts

  - id: agent-loop-exists
    type: file-exists
    path: supabase/functions/chat-api/agent-loop.ts

  - id: no-tool-takes-user-id
    type: grep
    path: supabase/functions/chat-api/tools/catalog.ts
    pattern: '"user_id"'
    expect: absent

  - id: old-router-removed
    type: grep
    path: supabase/functions/chat-api/index.ts
    pattern: 'detectIntent\('
    expect: absent

  - id: extraction-delegates-to-pipeline
    type: grep
    path: supabase/functions/chat-api/tools/handlers.ts
    pattern: 'recipe-pipeline/extract-only'
    expect: present

  # --- Build / lint / tests ---
  - id: lint-clean
    type: command
    run: npm run lint
    expect_exit: 0

  - id: build-clean
    type: command
    run: npm run build
    expect_exit: 0

  - id: vitest-passes
    type: command
    run: npm run test:run
    expect_exit: 0

  - id: agent-loop-unit-tests
    type: file-exists
    path: supabase/functions/chat-api/__tests__/agent-loop.test.ts

  - id: golden-fixture-exists
    type: file-exists
    path: supabase/functions/chat-api/__tests__/fixtures/golden.json

  - id: golden-routing-tests
    type: file-exists
    path: supabase/functions/chat-api/__tests__/golden-routing.test.ts

  - id: golden-routing-covers-buckets
    type: grep
    path: supabase/functions/chat-api/__tests__/golden-routing.test.ts
    pattern: 'single_intent_search bucket'
    expect: present

  - id: golden-routing-destructive-gate
    type: grep
    path: supabase/functions/chat-api/__tests__/golden-routing.test.ts
    pattern: 'pendingConfirmation gates'
    expect: present

  - id: golden-routing-injection
    type: grep
    path: supabase/functions/chat-api/__tests__/golden-routing.test.ts
    pattern: 'prompt-injection'
    expect: present

  - id: web-search-catalog-gated
    type: grep
    path: supabase/functions/chat-api/__tests__/agent-loop.test.ts
    pattern: 'omits web_search_recipe when WEB_SEARCH_API_KEY is unset'
    expect: present
```

## Manual Follow-up (non-blocking)

> Live model quality, latency, and cost — **after** `complete`. Never gates status.

- [ ] Tier 2 live golden eval with real OpenRouter (`RUN_LIVE_EVAL=1` script — not yet built)
- [ ] Median latency targets: ≤1.5s (no tools), ≤3.5s (1 tool), ≤6s (multi-tool)
- [ ] Median cost per turn ≤ $0.005
- [ ] `golden-web-search` bucket with live model (≥4/5 web_search → extract chain)
- [ ] Web-search snippet prompt-injection spot-check (≥2 cases)

---

_Removed from verification YAML (were `type: human`, blocked `complete`): `golden-single-intent`, `golden-multi-intent`, `golden-destructive-confirm`, `prompt-injection-resisted`, latency/cost/rls human gates, `web-search-graceful-when-unconfigured` (covered by `web-search-catalog-gated`), `golden-web-search`, `web-search-injection-resisted`._

---

## Acceptance Criteria

- [ ] All `verification` block items pass
- [ ] No frontend code calls an LLM directly (CLAUDE.md rule preserved)
- [ ] All prompts live in `_shared/recipe-prompts.ts`
- [ ] All tools use `auth.uid()`, none accept `user_id` as a parameter
- [ ] Destructive tools require confirmation (delete, update, clear-cart, bulk)
- [ ] Recipe extraction tool invokes existing `recipe-pipeline` (no duplicate extraction logic)
- [ ] Golden test set committed under `supabase/functions/chat-api/__tests__/`
- [ ] `cooking-bot-architect` design output committed at `docs/MOPs/MOP-0008-design.md`
- [ ] `lessons-learned.md` updated with proposed KB additions from architect output

---

## Notes

- **Why single agent, not supervisor.** Per `cooking-bot-knowledge/architecture-patterns.md`, multi-agent escalation requires measurement showing single-agent fails specific evals. We don't have that data yet. Phase 2 follows Phase 1 only if eval data justifies it.
- **Model choice.** Stay with Qwen 2.5 family (current chat-api default). Tool use is supported on Qwen 2.5 7B+ instruct variants. Final model + temperature settings pending architect output.
- **Latency budget.** Current chat-api responds in ~2-5s for general chat, ~3-8s for RAG. Tool-use loops can add multiple LLM hops — target: 95th percentile under 12s per user turn. If exceeded, revisit single-call vs multi-hop.
- **Cost budget.** Per-message cost will rise ~2-3x due to tool catalog tokens + tool result composition. Acceptable within Phase 1 if usage stays bounded (see `safety-and-guardrails.md` rate limits).
- **Dependency on MOP-0007.** Several tools wrap RAG RPCs. MOP-0007 (wiring those RPCs into the UI) does not block MOP-0008 — both can proceed in parallel; the tool implementations call the RPCs directly.
- **Dependency on MOP-0010.** The `verification` block uses the lockticket schema. Until MOP-0010 ships, items are checked by hand. Schema is forward-compatible.

---

## Addendum 1: `web_search_recipe` Tool (2026-06-01)

> **Why.** Triggered by user feedback: "I'm looking to go grab a devilled egg recipe from the web." The original 11-tool catalog requires the user to supply a URL (`extract_recipe_from_source`), leaving the very common "find a recipe online for me" path unaddressed. Adding a 12th read-only tool — `web_search_recipe` — lets the agent compose `web_search_recipe` → `extract_recipe_from_source` autonomously.
>
> This addendum **extends** [MOP-0008-design.md](MOP-0008-design.md); the design doc remains the source for tools 1–11. Bumps in this MOP's headers (Architectural Background, Step 5) reflect the new total.

### Catalog change

| | Before | After |
|---|---|---|
| Total tools | 11 | 12 |
| Read tools | 6 | 7 |
| Capture | 1 | 1 |
| Plan/cart | 2 | 2 |
| Destructive | 2 | 2 |

### Tool schema

```json
{
  "name": "web_search_recipe",
  "description": "Search the public web for recipes matching a query. Returns up to 5 candidate results (title, URL, source domain, snippet) ranked by relevance. Does NOT extract or save. Use when the user asks to find a recipe online, asks for inspiration outside their saved collection, or when `search_recipes` returns no matches. The agent typically chains a follow-up `extract_recipe_from_source` call with one of the returned URLs (either auto-selected when the top result is clearly relevant, or after presenting candidates to the user). Read-only.",
  "destructive": false,
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "minLength": 1,
        "description": "Natural-language recipe search, e.g. 'devilled eggs', 'gluten-free chocolate chip cookies', 'one-pot weeknight pasta'."
      },
      "max_results": {
        "type": "integer",
        "minimum": 1,
        "maximum": 5,
        "default": 3
      },
      "site_filter": {
        "type": "string",
        "description": "Optional domain to restrict the search to (e.g. 'seriouseats.com'). Use only when the user explicitly names a source."
      }
    },
    "required": ["query"],
    "additionalProperties": false
  }
}
```

### Tool result shape

Conforms to the catalog-wide `ToolResult` envelope from design.md Step 5.

```ts
type WebSearchRecipeResult =
  | {
      ok: true;
      data: {
        candidates: Array<{
          title: string;
          url: string;            // Suitable for direct pass-through to extract_recipe_from_source
          source_domain: string;  // Host portion, e.g. "seriouseats.com"
          snippet: string;        // ≤ 200-char excerpt for ranking display
        }>;
        provider: "tavily" | "brave" | "serper"; // Telemetry only
      };
    }
  | {
      ok: false;
      error:
        | "PROVIDER_UNAVAILABLE"
        | "RATE_LIMITED"
        | "NO_RESULTS"
        | "PROVIDER_NOT_CONFIGURED";
      retryable: boolean;
    };
```

### Provider abstraction

**New file:** `supabase/functions/_shared/web-search-client.ts`

- Default provider: **Tavily** (recipe-friendly relevance, ~$0.005/search, free tier ~1k queries/mo).
- Drop-in alternatives via the same interface: **Brave Search**, **Serper**.
- Provider selected via `WEB_SEARCH_PROVIDER` env var (default `tavily`).
- API key via `WEB_SEARCH_API_KEY` (read only inside the shared client; never referenced from handlers).
- **Capability gating:** if `WEB_SEARCH_API_KEY` is unset at function init, `web_search_recipe` is omitted from the agent's tool catalog at startup rather than failing at call time. Agent simply lacks the capability.

### Handler addition (Step 5)

Append to `supabase/functions/chat-api/tools/handlers.ts`:

- `web_search_recipe` — call shared `webSearch.query({ query, maxResults, siteFilter })`; map provider response to `WebSearchRecipeResult.data.candidates`. Map provider 5xx / timeouts to `{ ok: false, error: "PROVIDER_UNAVAILABLE", retryable: true }`. Empty results map to `{ ok: false, error: "NO_RESULTS", retryable: false }` so the agent can fall back to an explanation rather than retry.

No `ctx.supabase` access, no RLS surface, no DB writes.

### Agent prompt addition (Step 3)

Append to `CHAT_AGENT_SYSTEM_PROMPT`:

> When the user asks for a recipe and `search_recipes` returns no matches, OR the user explicitly asks to "find online" / "search the web" / "look up" / "grab" a recipe — call `web_search_recipe`, then `extract_recipe_from_source` with the most relevant URL. Surface the chosen source domain ("Found one from seriouseats.com — pulling it in now"). Never fabricate URLs; only use URLs returned by `web_search_recipe`. If the top candidate is not clearly the best match (multiple equally-relevant domains, ambiguous query, user asked for "options"), present the candidate list and ask which to extract.

### Verification additions

Append to the `verification:` YAML in the Verification block above:

```yaml
  # --- Addendum 1: web_search_recipe ---
  - id: web-search-tool-in-catalog
    type: grep
    path: supabase/functions/chat-api/tools/catalog.ts
    pattern: 'web_search_recipe'
    expect: present

  - id: web-search-handler-exists
    type: grep
    path: supabase/functions/chat-api/tools/handlers.ts
    pattern: 'web_search_recipe'
    expect: present

  - id: web-search-client-shared
    type: file-exists
    path: supabase/functions/_shared/web-search-client.ts

  - id: web-search-api-key-isolated
    type: grep
    path: supabase/functions/chat-api/tools/handlers.ts
    pattern: 'WEB_SEARCH_API_KEY'
    expect: absent
    # Key access lives only in the shared client.

  - id: web-search-catalog-gated-addendum
    type: grep
    path: supabase/functions/chat-api/__tests__/agent-loop.test.ts
    pattern: 'omits web_search_recipe when WEB_SEARCH_API_KEY is unset'
    expect: present
```

_Addendum 1 human gates (`web-search-graceful-when-unconfigured`, `golden-web-search`, `web-search-injection-resisted`) moved to Manual Follow-up above._

### Cost and latency impact

- **Cost.** Tavily ~$0.005/search. Turns that hit `web_search_recipe` will roughly double the per-turn budget from `cost-per-turn` ($0.005 → ~$0.010). Acceptable for an opt-in path; do not promote `web_search_recipe` over `search_recipes` for queries that can be served locally.
- **Latency.** +400–1200 ms per search call. The expected `web_search_recipe` → `extract_recipe_from_source` chain will dominate the `latency-multi-tool` bucket: target ≤ 8 s p50 for these specifically (vs. the original ≤ 6 s for purely-local multi-tool turns).

### Safety notes

- **Untrusted snippet text.** Search results are third-party. Snippet content lives inside the existing `<tool_result>...</tool_result>` markers (design.md Step 6) for prompt-injection isolation. Tested via `web-search-injection-resisted`.
- **URL surface.** Returned URLs flow into `extract_recipe_from_source` → `recipe-pipeline` URL adapter, which is already a path that hits external URLs. No new sandbox surface.
- **Provider failures.** Must not block other tool calls in the same loop iteration. Handler returns `ok: false` with `retryable` set; the agent surfaces the failure conversationally.

### Open question (resolve before implementation)

- **Auto-extract vs. ask-first.** Should the agent chain `web_search_recipe` → `extract_recipe_from_source` autonomously, or always present the candidate list and require selection? Phase 1 default: **chain autonomously when one candidate is clearly best (named domain, exact-title match); otherwise present candidates.** Revisit after golden evals — the `golden-web-search` bucket should include both kinds of prompts.
