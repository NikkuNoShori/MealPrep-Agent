import { ChatInterface } from "../components/chat/ChatInterface";

const Chat = () => {
  return (
    <div className="h-full overflow-hidden">
      <ChatInterface />
    </div>
  );
};

export default Chat;
